<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->

# NetFault engineering contract

Milestone 3F supersedes only 3E's prohibition on LAB 008/engine extension: one bounded EtherChannel investigation is now authorized and implemented. Read `docs/etherchannel-model.md` and `docs/milestone-3f.md` before changing it. Preserve the seven older scenario modules/revisions and all Academy content. No additional lab, Academy module, dependency, deployment, commit or push is authorized.

- Schema v7 permits exactly two switches, two same-subnet hosts without gateways, two inter-switch members, one same access VLAN and one local port channel per switch. Require explicit standalone-disable; reject alternate paths, unsupported channel fields and cross-switch VLAN differences. LACP does not negotiate remote VLAN IDs. Local group IDs need not match. Derive carrier, member eligibility, negotiation, logical state and forwarding separately; failed members must never fall through as independent links. One eligible member may sustain the group, but full repair verification checks intended membership. No STP, trunks, PAgP/static aggregation, timing, packets, hashing, bandwidth, min-links or arbitrary switching.
- Replay bounded group-mode edits from attempt repair events. Server-owned assessment reducers persist them through unchanged Blobs/CAS architecture; validate existing group/device/mode and enforce deadline/finalization/limits. Identical effective changes do not create versions. Observations retain repairIndex; initial evidence and latest-state recovery evidence have distinct purposes. Never grade only the selected remediation or mutate canonical scenarios.
- LAB 008 requires applied minimal repair plus fresh logical endpoint checks and reciprocal host pings for full credit. Feedback supports reasoning; detailed explanations, worked repair and all private lesson parts remain explicitly opened. Public catalog exposes only design intent and physical cabling, never authored operating mode, fault or repair target. Keep practice-pack inspectability and online-only assessment disclosures.
- LAB 008 adds `netfault.practice.etherchannel-01.v1`; optional repairs and repairIndex preserve version-1 journals. Retain old observations after changes with their configuration version labeled, and show changes in reopened feedback. Test desktop, 414px and 360px, production offline play, all eight lab regressions and static-asset/privacy boundaries. Source/vendor review is not physical IOS/Packet Tracer validation.

Milestone 3E boundaries: keep original OSPF teaching tables separate from all private case repairs. A network statement selects local interfaces; supplied contiguous wildcards and exact-address selectors are public conceptual practice. Keep router identity distinct from local process IDs, passive LAN advertisement distinct from adjacency, and transit point-to-point type explicit. Do not infer full reachability from one neighbor, route or on-link ping. The optional device companion stays UNEXECUTED until authentic environment/version/output evidence exists, separate from tap scoring. The small wildcard helper is restricted to authored /24, /30 and exact matches, never an IOS/configuration engine. No source-slide images, new dependencies, scenario/engine changes, storage redesign or worker-architecture changes are part of 3E.

Milestone 3D and its ANT follow-up are documentation-only audits. Read `docs/wia2008-sources.md` and its five companion deliverables before curriculum work. Historical-materials-based roadmap — 2026/27 course requirements unconfirmed. The follow-up inspected 11 local WIA2008 lecture decks (native text plus explicitly listed visual pages); originals/workbook were not found. Historical sources support the selected next proposal: one OSPFv2 configuration-to-verification Academy bridge, no new lab or engine change. This supersedes the provisional IPv6-first choice and current-materials intake gate. The learner does not yet have 2026/27 materials: do not ask again or block historical learning work on them. Milestone 3E separately authorized and implemented that one bridge; see docs/milestone-3e.md. Further curriculum work requires separate authorization. Preserve accepted 3C UX and published content. Keep source conflicts and partial-image limitations visible, never copy source decks into app assets, and never equate chapter or UM practical numbers with NetFault lab IDs. Do not relabel historical tests or user-reported device acceptance as fresh verification.

Scope: Milestone 3E adds only the ospfv2 module, ospfv2-configuration lesson, ospf-interface-guided and ospf-plan-independent exercises, all revision 1. Preserve the three revision-2 IPv4 lessons, six exercises, five diagrams and immutable revision-1 archive from 3C. The new lesson adds two diagrams and an optional UNEXECUTED external companion; no other module or lesson is authorized. Preserve all seven published labs, their revisions, engine, journals, practice packs and server-backed assessments. Do not add LAB 008, randomization, FlagForge, further modules, paid services or unrelated features. No commits, pushes, deployment or remote infrastructure changes without separate authorization. This nested project is independent of the parent portfolio and sibling CaptainOS.

Read `docs/milestone-3e.md`, `docs/learning-academy.md`, accepted `docs/milestone-3c.md`, and historical 3B record `docs/milestone-3b-plan.md` before extending Academy work. `docs/learning-academy-audit.md` and `docs/milestone-3a.md` are historical handoffs; 3B explicitly authorizes the bounded pilot previously deferred in 3A. Preserve the Next-generated block above.

- Milestone 3C: preserve `src/lib/academy/content-v1.ts` as immutable published content. Current IPv4 lessons/module/exercises are revision 2 with the same stable IDs; new OSPF content starts at revision 1. Resolve history by exact ID and revision, never by treating all revision-1 lessons as archived IPv4. Never migrate old numeric/IP drafts into new tap choices; keep original prompts, answer labels and stored feedback readable in revision history.
- New tap exercises use `tap-steps` and `requested-only` solutions. Wrong feedback gives reasoning guidance without the full solution. Require all fields, including subnet host-bit reasoning; retain plausible distractors. Keep native radio keyboard behavior, visible focus and 44px minimum card targets. No drag-only or typing-required standard path.
- Repeated identical checked answer sets must not create duplicate submissions/completion events. Explicit reveals stay idempotent. Changed retries are recorded without claiming unaided success after feedback. Preserve the v1 storage namespace/shape and old grading semantics.
- Diagrams are local HTML/CSS with text equivalents and computed bit/range examples. Keep the five IPv4 and two OSPF diagrams, preserve prose substance and all Field Guides, and test desktop plus 414px and 360px with production offline/touch workflows.
- Academy schema, catalog, exercise grading and progress remain separate from scenario/assessment state. Public related-lab metadata contains stable IDs and neutral relationships only. No private scenarios, repairs, evidence rules or hint imports.
- Keep stable module/lesson/exercise/reference IDs and immutable content revisions. New lesson revisions preserve earlier progress; changed exercises require new exercise revisions and a new lesson revision. Do not silently credit the new revision with old results.
- Keep all four original Field Guide bodies unchanged through the reference adapter. New lessons require the seven-part progression, analogy limits, intermediate worked reasoning, guided/independent interactive practice, requested-or-after-submission solutions and primary source references.
- Use deterministic validated structured answers. Preserve wrong attempts and feedback. Track explicit reveals separately; opening a solution is never exercise completion or independent success. Public Academy answers are inspectable; do not treat hidden UI as exam security.
- Academy progress uses `netfault.academy.progress.v1` only. Preserve raw corrupt/unknown records; surface quota/retention failures and exports. Keep old journal/pack keys intact. No cross-device sync or storage architecture replacement.
- Guard all Academy entry points during active assessments. Preserve server deadlines/history and practice diagnosis drafts/evidence. Related-lab links open mode selection without starting an attempt or fetching a private pack.
- Academy content stays in the eagerly cached public root shell. Verify actual current dependencies before displaying offline availability. APIs are never cached. No unsupported executable claims: `arp -a` is available only for LAB 003 PC-A, not LAB 002; Academy examples are conceptual.
- Run Academy schema/grading/progress regressions and all seven-lab tests, including production desktop/414px offline browser workflows. Never claim physical iPhone or live Netlify verification from local Chromium tests.

- Use strict TypeScript, deterministic scenario-driven state, validated versioned schemas and explicit device-appropriate supported commands. No LLM gameplay output or arbitrary free-text grading.
- The scenario is the source of truth. Derive neighbors, routes, connectivity and every command output from it. Read `docs/network-correctness.md` before changing network behavior. Explicit point-to-point transit type, passive LANs, unique router IDs, correct source/return routing and no accidental second fault are required.
- Preserve OSPF's PC-A–R1–R2–R3–PC-B topology/addressing and sole defect (R3 Gi0/0 area 1 against R2 area 0). Gateway lab is PC-A–SW1–R1–R2–PC-B: 192.168.10.0/24, 10.0.12.0/30, 192.168.20.0/24; only PC-A gateway .10.254 is wrong (repair .10.1). SW1 has unnumbered Layer 2 access ports in VLAN 10 and must never appear as an IP trace hop. All other configuration is healthy. Link colors must not reveal faults.
- Keep fault metadata, accepted repairs, evidence rules, hints and explanations in server-only scenario modules. Initial/active assessment payloads and static client assets must not contain private answers. Practice pack is explicitly downloaded, inspectable and offline-capable. Disclose that this is self-assessment, not tamper-proof testing.
- Assessment commands, evidence history, deadlines and final grades are server-owned. No hint/reveal during an active attempt. Expired or finalized sessions cannot be amended. Do not cache assessment APIs in the service worker.
- Grade structured cause, exact affected-device set, server-recorded evidence and accepted repair. Preserve OSPF's 30/20/30/20 rubric. Gateway repair requires the exact address and a structured forwarding explanation (10 points each). Notes are saved but not graded. Never claim language understanding from keywords.
- Preserve the journal fields and local storage versioning. Do not overwrite corrupt data or silently discard persistence failures. No destructive data changes, deployment, commits or pushes without explicit authorization.
- Teach concepts with all seven parts: simple, bounded analogy, technical, worked example, symptom, guided practice and independent exercise. Do not invent credentials, university endorsement, completion or mastery metrics.
- Keep mobile controls touch-sized, output panes readable/scrollable, focus visible and diagram alternatives accessible. Test 414 CSS pixels and desktop. Never claim physical iPhone testing or external network lab validation unless actually performed.
- Run `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm test:browser`, `pnpm build`, and production offline browser tests for relevant changes. Do not reuse a dev server for production service-worker testing. Record actual results, failures and limitations in `docs/verification.md`.
- Windows PowerShell is the supported local shell. Use pnpm in this folder. Do not delete sibling artifacts, change firewall/certificates, or expose the server publicly as part of routine tests.
- Review the architecture, scenario-authoring and iPhone documents before extending behavior. Keep the Next.js-generated instructions above intact.
- Read `docs/NETLIFY_DEPLOYMENT.md` and `docs/deployment-audit.md` before hosting changes. Use automatically managed OpenNext, Node 24, pinned pnpm packageManager and frozen lockfile. No static export, SPA fallback or pinned adapter plugin. Check current official Netlify documentation.
- Hosted assessment storage must remain durable across invocations: site-scoped Blobs, strong reads, conditional ETag writes and conflict re-evaluation. No cloud filesystem fallback or process-only locks. Preserve immutable final grades and server deadlines. Do not weaken ETag guards for the local SDK emulator, which currently omits GET ETags. No required application secrets; never expose credentials in NEXT_PUBLIC variables.
- Keep API responses no-store at browser/CDN/Netlify layers and out of the worker. Source worker is `scripts/service-worker.js`; build generates `public/sw.js` with a unique build ID. Install complete snapshots atomically, offer safe reloads and test real production offline/update behavior.
- No account connection, site linking, public deployment, paid infrastructure, commits or pushes without user authorization. Keep production and preview storage separate when previews are enabled. Record local-tooling limits separately from actual cloud or iPhone verification.
- Retain legacy schema-v1 OSPF packs and attempts; gateway scenario uses schema v2. Keep browser pack keys separate, with the original OSPF key unchanged. Server commands/grades always select the persisted attempt's scenario, never a later client override. Do not redesign the assessment storage architecture to add scenarios.
- Read `docs/gateway-lab.md` before changing the access-switch or host-forwarding model. Scenario exceptions for an intentionally missing gateway must be restricted to the authored fault; never globally disable gateway/address validation. Keep worked solutions and seven-part feedback server-only until submission (practice packs remain deliberately inspectable).
- Read `docs/vlan-lab.md` before changing LAB 003. Its sole fault is SW1 FastEthernet0/1 access VLAN 20 instead of 10. FastEthernet0/24 stays in VLAN 10; both VLANs and physical links are active. PC-A's .10.1 gateway and both routers are correct. VLAN reachability must depend on active same-VLAN paths, never a lab-ID failure flag.
- VLAN scenario uses schema v3 with a port-only repair and its own pack key. Keep v1/v2 packs and version-1 journals readable. New observations carry their scenario; legacy observations inherit their containing attempt's context. ARP is derived by replaying server-owned/practice history through the same forwarding function, not process memory or fabricated cache entries. New attempts/repair previews start empty, with no wall-clock aging or ambient traffic modeled.
- Preserve LAB 001/002 grading. LAB 003 grades cause/observed VLAN 20+10, device/interface 10+10, evidence 30, targeted access VLAN repair 20. A failed ping alone earns no evidence credit. Require host configuration plus both access memberships for full evidence credit. Keep the independent exercise solution behind an explicit learner reveal after completion.
- Read `docs/return-path-lab.md` before changing LAB 004. Schema v4 `return-01` uses correct hosts/switch and no OSPF. R1 has static 192.168.20.0/24 via 10.0.12.2; only R2's route to 192.168.10.0/24 via 10.0.12.1 is missing. No default route may conceal it. Repair changes only R2's static route list. Preserve all prior scenario data and v1 journals/pack keys.
- Derive static routes, longest-prefix selection, source address, request and reply paths from shared state. Static routes support directly linked router next hops only; no recursive static next hops, ECMP or configurable distances. C/L precede S precede O for identical prefixes. Ping's default router source is its outgoing interface; explicit sources must be active local interfaces and are persisted by the server. An error response or trace hop can be displayed only when it can return to the probe source. Do not equate request delivery with a successful ping.
- LAB 004 grading: cause 20 + destination prefix 10; exact device 20; evidence 30 (R1 route table 10, PC-A addressing together with R2 route table 20); correctly targeted static route 10 + structured reply explanation 10. Invalid command output is not evidence. Four progressive hints; independent lesson solution remains explicit-reveal. Notes are saved, never interpreted.
- Read `docs/passive-interface-lab.md` before changing LAB 005. Schema v5 `passive-01` uses the original three-router addressing, all area 0, matching 10/40 timers, MTU 1500, no authentication and explicit point-to-point transits. Its sole fault is R2 Gi0/1 passive=true. R1 Gi0/0 and R3 Gi0/1 are correctly passive broadcast LANs. Repair clones and changes only the faulty boolean; never infer network type from passive or prefix length.
- Passive OSPF prevents adjacency but still advertises its up OSPF-enabled subnet through other working adjacencies. Derive this from shared state, not scenario-ID conditions. Before LAB 005 repair R1 learns 10.0.23.0/30 via R2; R3 has only C/L. After repair reciprocal R2–R3 FULL/- and both remote LAN routes/pings work. Keep LAB 001's area-mismatch state unchanged. Only authentication `none` is modeled; legacy omission means none. Do not invent transient neighbor or passive-interface FSM states.
- LAB 005 grades cause 30, exact router/interface 10+10, evidence 30, targeted no-passive repair/Hello explanation 10+10. Full evidence requires R2's explicit passive configuration plus up-state, neighbor and R2/R3 routing context. Failed ping/missing neighbor alone cannot prove cause. Four progressive hints and seven-part teaching with explicit independent-answer reveal are required. Preserve source-aware ping history and existing server-owned grading; pack `netfault.practice.passive-01.v1` must coexist with all four older packs and v1 journals.
- Keep milestone 2D local only. No deployment, remote project changes, paid services, pushes or commits without separate authorization. Preserve Netlify Blobs/ETag architecture, cache safeguards and dependency versions.
- Milestone 3A readiness: previews use `src/lib/preview.ts` and an explicitly downloaded pack. No lab-specific repair targets/narration in initial client code. Public catalog and fresh assessment fields are allowlisted by tests; production scans enumerate authored private content and all static chunks. Practice remains inspectable; do not claim exam confidentiality.
- Evidence requirements and alternatives must be nonempty; the grader also rejects empty rules even if schema validation is bypassed. Keep evidence total 30. Neighbor Dead Time is floor(0.9 × configured Dead), a labeled bounded snapshot, never a live countdown.
- Read `docs/timer-lab.md` and `docs/next-hop-lab.md`. Schema v6 covers LAB 006/007 without modifying the old five scenario revisions. LAB 006's only fault is R3 Gi0/0 5/20 versus intended 10/40; repair only that profile. LAB 007's only fault is R2's 192.168.30.0/24 static next hop 10.0.12.1 instead of 10.0.23.2; the reachable wrong next hop remains installed and creates a bounded R1–R2 loop. Preserve source/return behavior and LAB 004 semantics.
- Use the exhaustive typed registry and case authoring contracts: IDs/catalog/commands/links must agree, fault references resolve, evidence is achievable, broken symptoms are observable and an exact minimal repair restores the independently expected healthy network. New content must not use scenario-ID forwarding shortcuts.
- New packs use `netfault.practice.timer-01.v1` and `netfault.practice.next-hop-01.v1`; old keys and v1 journals remain readable. Do not change published lab content or add variants without immutable revision handling. Do not redesign Blobs/CAS storage.
- `docs/learning-academy-audit.md` is a separately authorized future implementation plan, not permission to expand the four guides. Preserve the distinction between public conceptual teaching and private case answers; a collapsed solution is presentation, not answer security.
