<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->

# NetFault engineering contract

Scope: Milestones 1, 1.5, 2A, 2B, 2C and explicitly authorized 2D only: OSPF area mismatch, incorrect default gateway, access VLAN membership mismatch, LAB 004 missing return path and LAB 005 passive OSPF interface. Do not begin LAB 006, FlagForge or other roadmap work without explicit authorization. This nested folder is independent of the parent portfolio and sibling CaptainOS projects. Never modify their code to make NetFault work.

- Use strict TypeScript, deterministic scenario-driven state, validated versioned schemas and explicit device-appropriate supported commands. No LLM gameplay output or arbitrary free-text grading.
- The scenario is the source of truth. Derive neighbors, routes, connectivity and every command output from it. Read `docs/network-correctness.md` before changing network behavior. Explicit point-to-point transit type, passive LANs, unique router IDs, correct source/return routing and no accidental second fault are required.
- Preserve OSPF's PC-A–R1–R2–R3–PC-B topology/addressing and sole defect (R3 Gi0/0 area 1 against R2 area 0). Gateway lab is PC-A–SW1–R1–R2–PC-B: 192.168.10.0/24, 10.0.12.0/30, 192.168.20.0/24; only PC-A gateway .10.254 is wrong (repair .10.1). SW1 has unnumbered Layer 2 access ports in VLAN 10 and must never appear as an IP trace hop. All other configuration is healthy. Link colors must not reveal faults.
- Keep fault metadata, accepted repairs, evidence rules, hints and explanations in server-only scenario modules. Initial/active assessment payloads and static client assets must not contain private answers. Practice pack is explicitly downloaded, inspectable and offline-capable. Disclose that this is self-assessment, not tamper-proof testing.
- Assessment commands, evidence history, deadlines and final grades are server-owned. No hint/reveal during an active attempt. Expired or finalized sessions cannot be amended. Do not cache assessment APIs in the service worker.
- Grade structured cause, exact affected-device set, server-recorded evidence and accepted repair. Preserve OSPF's 30/20/30/20 rubric. Gateway repair requires the exact address and a structured forwarding explanation (10 points each). Notes are saved but not graded. Never claim language understanding from keywords.
- Preserve the journal fields and local storage versioning. Do not overwrite corrupt data or silently discard persistence failures. No destructive data changes, deployment, commits or pushes without explicit authorization.
- Teach concepts with all seven parts: simple, bounded analogy, technical, worked example, symptom, guided practice and independent exercise. Do not invent credentials, university endorsement, completion or mastery metrics.
- Keep mobile controls touch-sized, output panes readable/scrollable, focus visible and diagram alternatives accessible. Test 414 CSS pixels and desktop. Never claim physical iPhone testing or external network lab validation unless actually performed.
- Run `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm test:browser`, `pnpm build`, and production offline browser tests for relevant changes. Do not reuse a dev server for production service-worker testing. Record actual results, failures and limitations in `docs/verification.md`.
- Windows PowerShell is the supported local shell. Use pnpm in this folder. Do not delete sibling artifacts, change firewall/certificates, or expose the server publicly as part of routine tests.
- Review the architecture, scenario-authoring and iPhone documents before extending behavior. Keep the Next.js-generated instructions above intact.
- Read `docs/NETLIFY_DEPLOYMENT.md` and `docs/deployment-audit.md` before hosting changes. Use automatically managed OpenNext, Node 24, pinned pnpm packageManager and frozen lockfile. No static export, SPA fallback or pinned adapter plugin. Check current official Netlify documentation.
- Hosted assessment storage must remain durable across invocations: site-scoped Blobs, strong reads, conditional ETag writes and conflict re-evaluation. No cloud filesystem fallback or process-only locks. Preserve immutable final grades and server deadlines. Do not weaken ETag guards for the local SDK emulator, which currently omits GET ETags. No required application secrets; never expose credentials in NEXT_PUBLIC variables.
- Keep API responses no-store at browser/CDN/Netlify layers and out of the worker. Source worker is `scripts/service-worker.js`; build generates `public/sw.js` with a unique build ID. Install complete snapshots atomically, offer safe reloads and test real production offline/update behavior.
- No account connection, site linking, public deployment, paid infrastructure, commits or pushes without user authorization. Keep production and preview storage separate when previews are enabled. Record local-tooling limits separately from actual cloud or iPhone verification.
- Retain legacy schema-v1 OSPF packs and attempts; gateway scenario uses schema v2. Keep browser pack keys separate, with the original OSPF key unchanged. Server commands/grades always select the persisted attempt's scenario, never a later client override. Do not redesign the assessment storage architecture to add scenarios.
- Read `docs/gateway-lab.md` before changing the access-switch or host-forwarding model. Scenario exceptions for an intentionally missing gateway must be restricted to the authored fault; never globally disable gateway/address validation. Keep worked solutions and seven-part feedback server-only until submission (practice packs remain deliberately inspectable).
- Read `docs/vlan-lab.md` before changing LAB 003. Its sole fault is SW1 FastEthernet0/1 access VLAN 20 instead of 10. FastEthernet0/24 stays in VLAN 10; both VLANs and physical links are active. PC-A's .10.1 gateway and both routers are correct. VLAN reachability must depend on active same-VLAN paths, never a lab-ID failure flag.
- VLAN scenario uses schema v3 with a port-only repair and its own pack key. Keep v1/v2 packs and version-1 journals readable. New observations carry their scenario; legacy observations inherit their containing attempt's context. ARP is derived by replaying server-owned/practice history through the same forwarding function, not process memory or fabricated cache entries. New attempts/repair previews start empty, with no wall-clock aging or ambient traffic modeled.
- Preserve LAB 001/002 grading. LAB 003 grades cause/observed VLAN 20+10, device/interface 10+10, evidence 30, targeted access VLAN repair 20. A failed ping alone earns no evidence credit. Require host configuration plus both access memberships for full evidence credit. Keep the independent exercise solution behind an explicit learner reveal after completion.
- Read `docs/return-path-lab.md` before changing LAB 004. Schema v4 `return-01` uses correct hosts/switch and no OSPF. R1 has static 192.168.20.0/24 via 10.0.12.2; only R2's route to 192.168.10.0/24 via 10.0.12.1 is missing. No default route may conceal it. Repair changes only R2's static route list. Preserve all prior scenario data and v1 journals/pack keys.
- Derive static routes, longest-prefix selection, source address, request and reply paths from shared state. Static routes support directly linked router next hops only; no recursive static next hops, ECMP or configurable distances. C/L precede S precede O for identical prefixes. Ping's default router source is its outgoing interface; explicit sources must be active local interfaces and are persisted by the server. An error response or trace hop can be displayed only when it can return to the probe source. Do not equate request delivery with a successful ping.
- LAB 004 grading: cause 20 + destination prefix 10; exact device 20; evidence 30 (R1 route table 10, PC-A addressing together with R2 route table 20); correctly targeted static route 10 + structured reply explanation 10. Invalid command output is not evidence. Four progressive hints; independent lesson solution remains explicit-reveal. Notes are saved, never interpreted.
- Read `docs/passive-interface-lab.md` before changing LAB 005. Schema v5 `passive-01` uses the original three-router addressing, all area 0, matching 10/40 timers, MTU 1500, no authentication and explicit point-to-point transits. Its sole fault is R2 Gi0/1 passive=true. R1 Gi0/0 and R3 Gi0/1 are correctly passive broadcast LANs. Repair clones and changes only the faulty boolean; never infer network type from passive or prefix length.
- Passive OSPF prevents adjacency but still advertises its up OSPF-enabled subnet through other working adjacencies. Derive this from shared state, not scenario-ID conditions. Before LAB 005 repair R1 learns 10.0.23.0/30 via R2; R3 has only C/L. After repair reciprocal R2–R3 FULL/- and both remote LAN routes/pings work. Keep LAB 001's area-mismatch state unchanged. Only authentication `none` is modeled; legacy omission means none. Do not invent transient neighbor or passive-interface FSM states.
- LAB 005 grades cause 30, exact router/interface 10+10, evidence 30, targeted no-passive repair/Hello explanation 10+10. Full evidence requires R2's explicit passive configuration plus up-state, neighbor and R2/R3 routing context. Failed ping/missing neighbor alone cannot prove cause. Four progressive hints and seven-part teaching with explicit independent-answer reveal are required. Preserve source-aware ping history and existing server-owned grading; pack `netfault.practice.passive-01.v1` must coexist with all four older packs and v1 journals.
- Keep milestone 2D local only. No deployment, remote project changes, paid services, pushes or commits without separate authorization. Preserve Netlify Blobs/ETag architecture, cache safeguards and dependency versions.
